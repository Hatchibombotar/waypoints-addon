{
	"format_version": "1.8.0",
	"animations": {
		"animation.waypoint.default": {
			"loop": true,
			"bones": {
				"root": {
					"rotation": ["query.rotation_to_camera(0)", "query.rotation_to_camera(1)", 0],
					// "scale": "query.distance_from_camera / 20"
					"scale": "query.distance_from_camera > 10 ? query.distance_from_camera/10: 1"

				}
			}
		}
	}
}